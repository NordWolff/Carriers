include:
  - project: "future-diagnostics/infrastructure/includes"
    file: "/mvn-settings-template.yml"

stages:
  - create_mvn_set
 
create:mvn_settings:
  stage: create_mvn_set
  extends:
    - .create_mvn_settings

script:
  - mvn clean install -s settings.xml
